<h1>My Students</h1>

<%= link_to 'New student', new_teacher_student_path %>

<table>
  <tr>
    <th>Name</th>
    <th>Id</th>
    <th colspan="3"></th>
  </tr>
 
  <% @student.each do |student| %>
    <tr>
      <td><%= student.name %></td>
      <td><%= student.number %></td>
      <td><%= link_to 'Show ', teacher_student_path(student.teacher, student)%></td>
      <td><%= link_to 'Edit ', edit_teacher_student_path(student.teacher,student) %></td>
      <td><%= link_to 'Destroy', teacher_student_path(student.teacher,student),
      method: :delete, data: { confirm: 'Are you sure?' } %></td>
                        
    </tr>
  <% end %>
</table>

<p><%= link_to 'Back', teachers_path %></p>